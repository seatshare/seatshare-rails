<% content_for :page_title do %>Add User Alias<% end %>

<%= form_for @user_alias, url: user_aliases_path, html: {class: "form-horizontal", role: "form"} do |f| %>

  <fieldset>
    <legend>User Alias details</legend>

    <div class="form-group">
      <%= f.label :first_name, :class => 'col-md-3 control-label' %>
      <div class="col-md-9">
        <%= f.text_field :first_name, :class => 'form-control', :placeholder => 'John' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :last_name, :class => 'col-md-3 control-label' %>
      <div class="col-md-9">
        <%= f.text_field :last_name, :class => 'form-control', :placeholder => 'Doe' %>
      </div>
    </div>

  </fieldset>

  <div class="row">
    <div class="col-md-12">
        <p class="text-right">
          <%= f.submit 'Create User Alias', :class => 'btn btn-primary' %>
        </p>
    </div>
  </div>

<% end %>

<% content_for :page_footer do %>
<% end %>
