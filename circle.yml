machine:
  ruby:
    version: 2.3.1
database:
  override:
    - mv config/database.ci.yml config/database.yml
    - bundle exec rake db:create db:migrate --trace
deployment:
  staging:
    branch: master
    heroku:
      appname: seatshare-app
